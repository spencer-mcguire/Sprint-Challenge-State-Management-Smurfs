const initailState = [
  {
    name: "Brainey",
    age: 200,
    height: "5cm",
    id: 0
  }
];

const smurfReducer = (state = initailState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default smurfReducer;
